import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFFFF9933); // Saffron (Buttons, Headers)
  static const Color background = Color(0xFFFFF5E1); // Cream (Main Background)

  // Text Colors
  static const Color textPrimary = Color(
    0xFF4A2C00,
  ); // Dark Brown (High Contrast)
  static const Color textSecondary = Color(
    0xFF8D6E63,
  ); // Light Brown (Subtitles)
  static const Color white = Colors.white;

  // Status & UI
  static const Color success = Color(
    0xFF2E7D32,
  ); // Dark Green for "Active" status
  static const Color error = Color(0xFFD32F2F); // Red
  static const Color fieldBorder = Color(
    0xFFFFCC80,
  ); // Light Orange border for inputs
}
